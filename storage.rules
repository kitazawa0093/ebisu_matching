rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 認証済みユーザーが自分の identity_documents フォルダにのみアップロード・読み取り可能
    match /identity_documents/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
